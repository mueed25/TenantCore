# TenantCore
Production-grade multi-tenant backend built with NestJS, Prisma, PostgreSQL &amp; Redis. Features tenant isolation middleware, JWT auth, RBAC, and per-tenant rate limiting — the infrastructure layer underneath real SaaS products.
